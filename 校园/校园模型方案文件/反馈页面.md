请帮我设计一个网站反馈建议页面。

项目名称：
指路猫（Guide Cat）

项目定位：
校园导航公益项目，用于收集学生反馈、纠错和建议。

设计要求：

整体风格：
简洁、现代、年轻
与首页统一风格
圆角卡片设计
颜色鲜艳但舒服
绿色＋蓝色为主

页面标题：

帮助我们变得更好

副标题：

发现错误、信息过期或有建议？
欢迎提交反馈。

————————

页面结构：

【联系方式】

微信：z18997728164
WA Business：＋60 11-6997 3050
QQ邮箱：2136541070@qq.com(邮箱可能看不到)


————————

【问题描述】

大文本输入框

占位文字：

请描述问题，例如：
签证办公室位置错误。

支持自动扩展输入。

————————

【页面位置（可选）】

下拉框：

首页
办公室
地图
签证
学费
宿舍
其他

————————

【联系方式（选填）】

输入：

邮箱
微信
Telegram

显示：

不填写也可以提交。

————————

【截图上传】

上传区域：

拖拽上传

提示：

支持 JPG PNG

————————

【提交按钮】

按钮：

提交反馈

提交成功后：

出现动画：

🐱
感谢反馈！

显示：

预计3天内更新。

————————

底部：

查看已修复问题

显示：

最近更新记录

示例：

✓ 修复 Finance 办公室位置
✓ 更新签证流程

————————

技术要求：

HTML
CSS
JavaScript

响应式设计

数据先保存到浏览器 localStorage

预留未来连接数据库接口。
检查电脑本机局域网地址，准备给你一个可在手机上访问的局域网链接。

Ran terminal command: cd /d "c:\Users\15319\Desktop\校园模型方案" ; python - <<'PY'
import socket
import re
ips=set()
for iface in socket.getaddrinfo(socket.gethostname(), None):
    ip=iface[4][0]
    if ':' in ip: continue
    if ip.startswith('127.') or ip.startswith('169.254.'): continue
    ips.add(ip)
print('\n'.join(sorted(ips)) or 'NO_IP')
PY

现在查找本机局域网 IP，并准备给出手机可访问的本地链接。

Ran terminal command: cd /d "c:\Users\15319\Desktop\校园模型方案"; python -c "import socket; ips=set();
for iface in socket.getaddrinfo(socket.gethostname(), None):
    ip=iface[4][0]
    if ':' in ip: continue
    if ip.startswith('127.') or ip.startswith('169.254.'): continue
    ips.add(ip)
print('\n'.join(sorted(ips)) if ips else 'NO_IP')"

命令失败是 PowerShell 路径语法问题，我改用 `Set-Location -LiteralPath` 重新读取本机 IP。

Ran terminal command: Set-Location -LiteralPath 'c:\Users\15319\Desktop\校园模型方案'; python -c "import socket; ips=set();
for iface in socket.getaddrinfo(socket.gethostname(), None):
    ip=iface[4][0]
    if ':' in ip: continue
    if ip.startswith('127.') or ip.startswith('169.254.'): continue
    ips.add(ip)
print('\n'.join(sorted(ips)) if ips else 'NO_IP')"

继续检查本机网络配置，确定可用的局域网地址。

Ran terminal command: Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -notlike '127.*' -and $_.IPAddress -notlike '169.254.*' } | Select-Object -ExpandProperty IPAddress

## 手机可访问链接

首先，在电脑上启动一个本地静态服务器（同一局域网内手机可访问）：

```powershell
cd /d "c:\Users\15319\Desktop\校园模型方案"
python -m http.server 8000
```

然后在手机浏览器打开：

- `http://10.141.189.209:8000/反馈建议.html`

如果手机浏览器对中文路径有问题，可以使用这个编码链接：

- `http://10.141.189.209:8000/%E5%8F%8D%E9%A6%88%E5%BB%BA%E8%AE%AE.html`

> 需要手机和电脑在同一个 Wi-Fi/局域网内。
-
​ 
http://10.141.189.209:8000/反馈建议.html